services:
  - type: static
    name: hutta-home-services
    buildCommand: echo "Static site - no build required"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /
        destination: /index.html